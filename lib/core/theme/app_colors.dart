import 'package:flutter/material.dart';

class AppColors {
  // Light Mode Background
  static const lightBgPrimary = Color(0xFFF8FAFC);
  static const lightBgSecondary = Color(0xFFFFFFFF);

  // Dark Mode Background
  static const darkBgPrimary = Color(0xFF020617);
  static const darkBgSecondary = Color(0xFF0F172A);

  // Gradients - Light
  static const lightGradPrimary = [Color(0xFFC4B5FD), Color(0xFFFDBA74)];
  static const lightGradSecondary = [Color(0xFFA5F3FC), Color(0xFFC084FC)];
  static const lightGradAccent = [Color(0xFFFDE68A), Color(0xFFFCA5A5)];

  // Gradients - Dark
  static const darkGradPrimary = [Color(0xFF6366F1), Color(0xFF22D3EE)];
  static const darkGradSecondary = [Color(0xFFA78BFA), Color(0xFF34D399)];

  // Text - Light
  static const lightTextPrimary = Color(0xFF0F172A);
  static const lightTextSecondary = Color(0xFF475569);
  static const lightTextDisabled = Color(0xFF94A3B8);

  // Text - Dark
  static const darkTextPrimary = Color(0xFFF8FAFC);
  static const darkTextSecondary = Color(0xFFCBD5E1);
  static const darkTextDisabled = Color(0xFF64748B);

  // Status
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);
}
